# Root-level secrets and local junk (do not publish)
client_secret*.json
*.pem
composer.phar
composer-setup.php
.DS_Store
Thumbs.db

# Optional: large/generated assets at repo root
ChatGPT*.png

# Згенерований повний пакет для FTP/хостингу (великий обсяг)
hosting/deploy-package/
hosting/deploy-package*.zip
hosting/*.zip
