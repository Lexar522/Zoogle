# Під час `php artisan --env=testing` ці значення доповнюють/перекривають `.env`.
# Обов’язково окрема БД для тестів — інакше migrate:fresh --env=testing зітре робочу базу з `.env`.
DB_DATABASE=sitezoo_test
